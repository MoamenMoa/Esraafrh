# ابدأ من هنا

هذه النسخة لا تعمل في وضع تجربة. لكي تدخل وتحفظ المشاريع فعليًا، نفّذ الخطوات بالترتيب:

1. أنشئ مشروع Firebase.
2. فعّل **Authentication → Email/Password**.
3. أنشئ الحسابات الثلاثة المذكورة أدناه.
4. أنشئ **Cloud Firestore**.
5. انشر محتوى `firestore.rules`.
6. انسخ بيانات Firebase Web App إلى `firebase-config.js`.
7. ارفع ملفات هذا المجلد كاملة إلى GitHub.
8. شغّل GitHub Pages من فرع `main` ومجلد `/(root)`.
9. أضف نطاق GitHub Pages إلى Firebase Authorized domains.
10. ادخل بحساب المدير أولًا؛ سيُنشأ مشروع هليوبوليس تلقائيًا إذا كانت القاعدة فارغة.

## الحسابات المطلوبة

أنشئها داخل **Firebase Authentication → Users** بالإيميلات التالية، واختر كلمات مرور قوية من عندك:

| الصلاحية | اسم الدخول داخل الموقع | البريد داخل Firebase |
|---|---|---|
| المدير | Esraafrh | esraafrh@example.com |
| مستخدم | Mohamed Elnshwi | mohamed.elnshwi@example.com |
| مستخدم | AhmedNagib | ahmed.nagib@example.com |

لا تكتب كلمات المرور داخل ملفات GitHub.

## بعد التشغيل

- المدير ينشئ المشاريع ويضيف البنود يدويًا أو من Excel.
- كل مشروع جديد يظهر لكل المستخدمين.
- المستخدم يضغط **✓ ابدأ العمل** من بطاقة المشروع أو داخله.
- اسم المستخدم ووقت البداية يظهران للجميع.
- المستخدم الذي استلم المشروع والمدير فقط يستطيعان إنهاء البنود وإضافة الملاحظات والمصروفات.
- صورة الفاتورة تُرفع مع المصروف ويمكن فتحها وتحميلها لاحقًا.
